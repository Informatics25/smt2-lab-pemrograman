#include <bits/stdc++.h>
using namespace std;

void solve() {
    // Solusi pengerjaan soal diletakkan di sini
}

int main() {
    // Optimasi I/O agar tidak TLE
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int t;
    // Hapus atau sesuaikan bagian ini jika soal tidak menggunakan jumlah test case (t)
    if (cin >> t) {
        while (t--) {
            solve();
        }
    }
    return 0;
}